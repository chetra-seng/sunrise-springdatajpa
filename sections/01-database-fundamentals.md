---
layout: center
---
# Before We Write Any Java

---

# Why SQL First?

Everything we'll do with Spring Data JPA is SQL under the hood.

If you know the SQL, you'll understand exactly what JPA is doing for you — and you'll know how to debug it when something goes wrong.

---

# What We'll Cover

| Section | Topics |
|---------|--------|
| Databases & PostgreSQL | What a database is, how PostgreSQL works |
| CRUD | SELECT, INSERT, UPDATE, DELETE |
| Filtering & Sorting | WHERE, ORDER BY, LIKE, LIMIT |
| Primary Keys | BIGSERIAL, auto-increment, sequences |
| Foreign Keys & JOINs | Relationships between tables |
| Transactions & Indexes | Data integrity, query performance |
